<%= render 'shared/content_header', title: "Edit expense: #{@expense.name}" %>

<%= render 'form' %>

<%= link_to "Delete", [@account, @expense], method: :delete, data: { confirm: "Are you sure?" } %>